<Window x:Class="JX.WPFToolkit.Windows.InputValuesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:JX.WPFToolkit.Controls"
        Title="Input Values" Height="300" Width="350"
		WindowStyle="ToolWindow"
		WindowStartupLocation="CenterScreen"
		Topmost="True">

	<Window.Resources>
		<ResourceDictionary Source="../Dictionaries/BaseDictionary.xaml" />
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<controls:InputValuesControl x:Name="InputValues" Margin="10" />

		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="Guardar" Width="50" IsDefault="True" Click="OnSaveClicked"/>
			<Button  Content="Cerrar" Width="50" Click="OnCloseClicked"/>
		</StackPanel>


	</Grid>

</Window>
